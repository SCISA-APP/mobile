import { Counselor, Booking } from '../models';

export interface CounselorsScreenProps {
  // No props needed for this screen
}

export interface CounselorDetailsScreenProps {
  // No props needed for this screen
}

export interface BookingsScreenProps {
  // No props needed for this screen
}

export interface BookingDetailsScreenProps {
  // No props needed for this screen
}
