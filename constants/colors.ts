export interface AppColors {
  background: string;
  primary: string;
  text: string;
  muted: string;
}

const colors: AppColors = {
  background: '#FFFFFF',
  primary: '#2B4C83',
  text: '#333333',
  muted: '#666666',
};

export default colors;
